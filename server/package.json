{
  "name": "tutien-server",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test tests/*.test.cjs"
  },
  "devDependencies": {
    "typescript": "5.7.3"
  }
}
